import 'package:count/widgets/button.dart';
import 'package:flutter/material.dart';
import './widgets/button.dart';

class App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(body: ButtonCreate()),
    );
  }
}
